# Content ful

content ful ia a headless CMS. THat stores content as a structured wat and deliver it as apis. Unlike traditional CMS. That doesn't have frontend. Manages delivery of the content

# Headless CMS

Headless CMS separates content layer from the presentation layer. It provides only APIS without UI rendering layer so content can be delivered anywhere
